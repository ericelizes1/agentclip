'''URL routes owned by the slideshows app.

Filled in by the views commit. Stubbed here so the project's top-level
``include('slideshows.urls')`` resolves while the scaffold lands.
'''

from django.urls import path

urlpatterns: list[path] = []
